# 失控进化 (com.tencent.rmcn) 绘制数据文档
作者：泪心
 QQ：2254013571
 百分百已退网，勿扰

> 版本: 1.96.1.1333 | 引擎: Unity (HybridCLR) | 架构: ARM64

---

## 一、已知偏移链（实测数据）

### 1. 世界数组（Entity列表）

```
libunity.so:bss[1]
[Cb]+0x79be8+0x668+0x4c8-0x38+0x80+0x70+0x78
```

### 2. 矩阵（ViewProjection Matrix）

```
libunity.so:bss
+0xABB80+0x40+0x10+0x110
```

**用途**: 世界坐标转屏幕坐标，ESP绘制核心

```
// W2S 计算
ScreenPos = ViewProjectionMatrix * WorldPos
if (ScreenPos.w > 0) {
    X = (ScreenPos.x / ScreenPos.w + 1) * ScreenWidth / 2
    Y = (1 - ScreenPos.y / ScreenPos.w) * ScreenHeight / 2
}
```

### 3. 人物数组

```
libunity.so:bss[1]
[Cb]+0x8cd40+0x128+0x4d0+0x410+0x5f0+0x718+0x10+0x18+0x0
```

---

## 二、PlayerEntity 核心结构（C#类反编译）

> 类名: `WizardGames.Soc.Common.Entity.PlayerEntity`
> 继承: `EntityBase` -> `CustomTypeBase`
> 源码位置: com.tencent.rmcn_1.96.1.1333..cs 第494289行

### 坐标相关（核心！绘制方框必备）

PlayerEntity 实现了 `IPositionEntity` 接口，坐标通过 `PosX`/`PosY`/`PosZ` 属性存取。

**方式一：直接读 Smooth 坐标（推荐，最稳定）**

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `_PosX_Smooth` | 0x5C | float | **平滑后X坐标** |
| `_PosY_Smooth` | 0x60 | float | **平滑后Y坐标** |
| `_PosZ_Smooth` | 0x64 | float | **平滑后Z坐标** |

```c
// 读取玩家坐标 - 直接读内存
float posX = *(float*)(playerEntity + 0x5C);  // X
float posY = *(float*)(playerEntity + 0x60);  // Y (高度)
float posZ = *(float*)(playerEntity + 0x64);  // Z
```

**方式二：通过组件系统读原始坐标（更精确但更复杂）**

| 接口 | 方法 | 说明 |
|------|------|------|
| IPosition2Entity | `get_PosX()` | 原始X坐标 |
| IPosition2Entity | `get_PosZ()` | 原始Z坐标 |
| IPositionEntity | `get_PosY()` | 原始Y坐标（高度） |

坐标通过 EntityBase 的 `components` 字典（偏移0x40）查找 DamageableComponent 等组件间接获取。IL2CPP 编译后可通过虚函数表调用 `get_PosX/Y/Z`。

**方式三：通过 GameObject Transform（最准但需要Go指针）**

```
BasePlayerGo + 0x68 = PlayerEntity
BasePlayerGo -> RootGameObject -> Transform -> Position
```

### 旋转/朝向（射线绘制必备）

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `_RotateX_Smooth` | 0x90 | float | 平滑后旋转X |
| `_RotateY_Smooth` | 0x94 | float | **平滑后旋转Y（Yaw/朝向）** |
| `_RotateZ_Smooth` | 0x98 | float | 平滑后旋转Z |
| `LastRotateY` | — | float | 上一次Yaw |
| `AimYaw` | 0x58C | float | 瞄准偏航 |
| `MyCmdPitch` | 0x590 | float | 指令俯仰 |

### 速度向量（移动方向）

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `SpeedX` | — | float | X方向速度 |
| `SpeedY` | — | float | Y方向速度 |
| `SpeedZ` | — | float | Z方向速度 |
| `WantSpeedX` | — | float | 期望X速度 |
| `WantSpeedY` | — | float | 期望Y速度 |
| `WantSpeedZ` | — | float | 期望Z速度 |
| `ExtraSpeedX` | — | float | 额外X速度 |
| `ExtraSpeedY` | — | float | 额外Y速度 |
| `ExtraSpeedZ` | — | float | 额外Z速度 |
| `AddSpeed` | 0x2028 | Vector3 | 叠加速度 |
| `DMoveSpeed` | 0x2010 | Vector3 | 移动速度向量 |

### 历史坐标（计算距离/速度用）

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `LastPosX` | 0x1A0 | float | 上一次X坐标 |
| `LastPosZ` | 0x1A4 | float | 上一次Z坐标 |
| `LastPosTime` | 0x1A8 | int64 | 上一次坐标时间 |
| `PlatformPosX_Smooth` | 0x9C | float | 平台X平滑坐标 |
| `PlatformPosY_Smooth` | 0xA0 | float | 平台Y平滑坐标 |
| `PlatformPosZ_Smooth` | 0xA4 | float | 平台Z平滑坐标 |

### 血量/生命相关

PlayerEntity 继承了 `ICombatEntity`、`ILifeCycleEntity` 接口，血量存储在 **DamageableComponent** 组件中：

| 组件/字段 | 偏移 | 类型 | 说明 |
|-----------|------|------|------|
| DamageableComponent (组件) | — | Component | 血量组件 |
| DamageableComponent.MaxHp | 0x? | float | 最大血量 |
| DamageableComponent.Hp | 0x? | float | 当前血量 |
| DamageableComponent.DefenseTemplateId | 0x34 | int32 | 防御模板ID |
| DamageableComponent.BaseProtection | 0x38 | float[] | 基础护甲 |
| DamageableComponent.IsInvincible | — | bool | 是否无敌 |
| `_health` (EntityBase) | 0x110 | float | 基础血量值 |
| `_maxHealth` (EntityBase) | 0x114 | float | 最大血量值 |

### 死亡/倒地状态

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `IsDead` | 0x10D | bool | 是否死亡 |
| `<IsInFire>k__BackingField` | 0x179 | bool | 是否在开火 |
| `<DyingFinishTime>k__BackingField` | 0x168 | int64 | 倒地完成时间 |
| `<EnterDyingTime>k__BackingField` | 0x1B8 | float | 进入倒地时间 |
| `<LastLifeCycleFlags>k__BackingField` | 0x1B0 | int32 | 上一生命周期标志 |
| `<LastIsWounded>k__BackingField` | 0x1B4 | bool | 上次是否受伤 |
| `<RebornRequest>k__BackingField` | 0x1BC | enum | 复活请求类型 |

### 移动状态

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `MoveState` | — | enum (byte) | 移动状态（见枚举表） |
| `PoseState` | — | enum (byte) | 姿势状态（见枚举表） |
| `<IsInAir>k__BackingField` | 0x350 | bool | 是否在空中 |
| `SlopeAngle` | 0x110 | float | 坡度角 |
| `InSlope` | 0x120 | bool | 是否在斜坡上 |
| `IsContinuousJump` | 0x663 | bool | 是否连续跳跃 |

#### PlayerMoveStateEnum 枚举

| 值 | 名称 | 说明 |
|----|------|------|
| 0 | MoveIdle | 站立不动 |
| 1 | Run | 跑步 |
| 2 | Sprint | 冲刺 |
| 3 | Jump | 跳跃 |
| 4 | Fall | 下落 |
| 5 | MoveMantle | 攀爬 |
| 6 | MoveLadder | 爬梯子 |
| 7 | Fly | 飞行 |
| 8 | Observer | 观察者 |
| 9 | MoveSwim | 游泳 |
| 10 | MoveZipline | 滑索 |
| 11 | Mountable | 骑乘 |

#### PlayerPoseStateEnum 枚举

| 值 | 名称 | 说明 |
|----|------|------|
| 0 | Stand | 站立 |
| 1 | Crouch | 蹲下 |
| 2 | Dying | 倒地 |
| 3 | Swim | 游泳 |
| 4 | Ladder | 爬梯 |
| 5 | Dive | 潜水 |
| 6 | Mantle | 攀爬 |
| 7 | Zipline | 滑索 |
| 8 | Drive | 驾驶 |
| 9 | Max | 最大值 |

#### PlayerMoveLadderStateEnum 枚举

| 值 | 名称 | 说明 |
|----|------|------|
| 0 | LadderNone | 不在梯子上 |
| 1 | InLadder | 在梯子上 |
| 2 | BleedInLadder | 进入梯子 |
| 3 | BleedOutLadder | 离开梯子 |

### 武器/手持相关

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `NextWeaponId` | 0x280 | int64 | 下一个武器ID |
| `PreviousWeaponTableId` | 0x288 | int64 | 上一个武器表ID |
| `<NowHistoryWeaponId>k__BackingField` | 0x290 | int64 | 当前历史武器ID |
| `<IsInFire>k__BackingField` | 0x179 | bool | **是否在开火** |
| `<IsAutoFire>k__BackingField` | 0x2D4 | bool | 是否自动开火 |
| `<CanAutoFire>k__BackingField` | 0x2D5 | bool | 是否可以自动开火 |
| `<FireToggle>k__BackingField` | 0x310 | bool | 开火切换 |
| `<ThrowToggle>k__BackingField` | 0x311 | bool | 投掷切换 |
| `<IsStepReload>k__BackingField` | 0x160 | bool | 是否分步换弹 |
| `<ReloadStartTime>k__BackingField` | 0x15C | int32 | 换弹开始时间 |
| `ammoInBag` | 0xC8 | int32 | 背包弹药数 |
| `<HeldItemSwitchSeq>k__BackingField` | 0x108 | int32 | 手持物品切换序号 |
| `<BowWeapon_FireTime>k__BackingField` | 0x170 | int64 | 弓箭开火时间 |
| `eWeaponColor` | 0x574 | int32 | 武器颜色（品质） |
| `FpAnimIndex` | 0x638 | WeaponUnique | FP动画索引 |
| `ApplyFpAnimIndex` | 0x644 | WeaponUnique | 应用FP动画索引 |

### 瞄准/开镜相关

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `<AdsLerpPosition>k__BackingField` | 0x260 | float | 开镜插值位置 |
| `<AdsAnimRawProgress>k__BackingField` | 0x264 | float | 开镜动画原始进度 |
| `<AdsAnimState>k__BackingField` | 0x268 | enum | **开镜动画状态** |
| `<AdsAnimStateStartTime>k__BackingField` | 0x26C | int32 | 开镜状态开始时间 |
| `<AdsAnimPosition>k__BackingField` | 0x270 | float | 开镜动画位置 |
| `<AdsSwitchTime>k__BackingField` | 0x278 | int64 | 开镜切换时间 |
| `<GunFov>k__BackingField` | 0x2A4 | float | 枪械FOV |
| `<AdsFovOnTime>k__BackingField` | 0x2A8 | int32 | 开镜FOV开启时间 |
| `<AdsFovOffTime>k__BackingField` | 0x2AC | int32 | 开镜FOV关闭时间 |
| `defaultSceneFov` | 0x298 | float | 默认场景FOV |
| `defaultVehicleFov` | 0x29C | float | 默认载具FOV |
| `AimYaw` | 0x58C | float | 瞄准偏航 |
| `MyCmdPitch` | 0x590 | float | 指令俯仰 |

#### ADSAnimState（开镜状态判断）

通过 `AdsAnimState` 可以判断：
- 开镜中 / 关镜中 / 已开镜 / 已关镜

### 生存属性（代谢系统）

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `<OxygenDelta>k__BackingField` | 0x1C4 | float | 氧气变化 |
| `<HydrationFraction>k__BackingField` | 0x1CC | float | 水分比例 |
| `<HeartRateFraction>k__BackingField` | 0x1D0 | float | 心率比例 |
| `<TargetTemperature>k__BackingField` | 0x1D4 | float | 目标温度 |
| `<TargetComfort>k__BackingField` | 0x1E0 | float | 目标舒适度 |
| `<CarloriesFraction>k__BackingField` | 0x1E8 | float | 卡路里比例 |
| `<BleedingCache>k__BackingField` | 0x1F4 | float | 流血缓存 |
| `<RadiationPoisonFraction>k__BackingField` | 0x1F8 | float | 辐射中毒比例 |
| `<RadiationLevelFraction>k__BackingField` | 0x1FC | float | 辐射等级比例 |
| `<WetnessFraction>k__BackingField` | 0x204 | float | 湿度比例 |
| `<WaterLevel>k__BackingField` | 0x208 | float | 水位 |
| `<OxygenDamageCache>k__BackingField` | 0x1DC | float | 缺氧伤害 |
| `<TemperDamageCache>k__BackingField` | 0x1D8 | float | 温度伤害 |
| `<HydrationDamageCache>k__BackingField` | 0x1E4 | float | 脱水伤害 |
| `<CaloriesDamageCache>k__BackingField` | 0x1EC | float | 饥饿伤害 |

### 敌意/红名

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `<lastIsHostile>k__BackingField` | 0x330 | bool | 上次是否敌对 |
| `<lastRedName>k__BackingField` | 0x331 | bool | 上次是否红名 |
| `<lastHostileStartTime>k__BackingField` | 0x338 | int64 | 敌对开始时间 |
| `<lastHostileEndTime>k__BackingField` | 0x340 | int64 | 敌对结束时间 |
| `<MarkAttackerHostile>k__BackingField` | 0x348 | bool | 标记攻击者为敌对 |
| `<lastSafetyState>k__BackingField` | 0x34C | int32 | 上次安全区状态 |
| `<LastAttackedTime>k__BackingField` | 0x210 | int64 | 上次被攻击时间 |
| `<LastAttackPlayerId>k__BackingField` | 0x218 | int64 | 上次攻击玩家ID |

### 其他

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `<TemplateId>k__BackingField` | 0x328 | int64 | 角色模板ID |
| `GlobalInvincible` | (static 0x40) | bool | 全局无敌开关 |
| `<DrawScale>k__BackingField` | 0x354 | float | 拔枪缩放 |
| `<FireScale>k__BackingField` | 0x358 | float | 开火缩放 |
| `<IsWalkableGround>k__BackingField` | 0x468 | bool | 是否可行走地面 |
| `<bLocalPlayer>k__BackingField` | 0x2C0 | bool | 是否本地玩家 |
| `<teleport>k__BackingField` | 0x351 | bool | 是否传送 |

---

## 三、ClientPlayerGo 结构（第三方玩家视图对象）

> 类名: `WizardGames.Soc.Common.Unity.Go.ClientPlayerGo`
> 继承: `BasePlayerGo` -> `BaseEntityGo`
> 源码位置: 第530669行

### 骨骼/骨骼点（用于骨骼ESP绘制）

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `HeadBone` | 0xD8 | Transform | 头部骨骼 |
| `BodyBone` | 0xE0 | Transform | 身体骨骼 |
| `SpineBone` | 0xE8 | Transform | 脊椎骨骼 |
| `LeftFootBone` | 0xF0 | Transform | 左脚骨骼 |
| `RightFootBone` | 0xF8 | Transform | 右脚骨骼 |
| `Bip01Neck` | 0x100 | Transform | 脖子骨骼 |
| `SocCharacterController` | 0x140 | CharacterController | 角色控制器 |
| `PlayerEntity` (继承自BasePlayerGo) | 0x68 | PlayerEntity | 对应的PlayerEntity |

### 手持武器视觉

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `BackWeaponMgr` | 0xB0 | BackWeaponMgr | 背后武器管理器 |
| `<HostHeldItemGoController>k__BackingField` | 0x1F0 | PlayerHostHeldItemGoController | 手持武器控制器 |

---

## 四、ClientPlayerFpGo 结构（第一人称视图对象）

> 类名: `WizardGames.Soc.Common.Unity.Go.ClientPlayerFpGo`
> 源码位置: 第530584行

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `<skeletonGo>k__BackingField` | 0x90 | GameObject | 骨骼对象 |
| `PartModelController` | 0x98 | IPartModelController | 部位模型控制器 |
| `<FpCameraLocator>k__BackingField` | 0xA8 | Transform | FP相机定位点 |
| `<NeckBone>k__BackingField` | 0xB0 | Transform | 脖子骨骼 |
| `<HeldItemGoController>k__BackingField` | 0xF0 | PlayerHostFpHeldItemGoController | FP手持物品控制器 |

---

## 五、DamageableComponent 结构（血量组件）

> 类名: `WizardGames.Soc.Common.Component.DamageableComponent`
> 继承: `ComponentBase`
> 源码位置: 第514161行

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `<DefenseTemplateId>k__BackingField` | 0x34 | int32 | 防御模板ID |
| `<BaseProtection>k__BackingField` | 0x38 | float[] | 基础护甲值 |
| `<AreaProtections>k__BackingField` | 0x40 | float[][] | 区域护甲值 |
| `<ScaleDamageByPart>k__BackingField` | 0x48 | bool | 是否按部位计算伤害 |

**子类**:
- `PlayerDamageableComponent` — 玩家血量组件
- `VehicleDamageableComponent` — 载具血量组件
- `BoxDamageableComponent` — 箱子血量组件
- `ModularCarDamageableComponent` — 模块化汽车血量组件
- `PartDamageableComponent` — 部位血量组件

---

## 六、HeldItemCustom 结构（手持物品/武器数据）

> 类名: `WizardGames.Soc.Common.CustomType.HeldItemCustom`
> 继承: `CustomBaseItem`
> 源码位置: 第505303行

| 字段名 | 偏移 | 类型 | 说明 |
|--------|------|------|------|
| `NextFireTime` | — | int64 | 下次开火时间 |
| `CollectedContent` | — | int32 | 收集内容 |
| `IsReloading` | 0x70 (相关类) | bool | 是否换弹中 |

---

## 七、GunBase 数据表（武器配置）

> 类名: `WizardGames.Soc.Common.Data.gun.GunBase`
> 源码位置: 第364829行

| 子结构 | 说明 |
|--------|------|
| GunBase | 武器基础数据 |
| GunEffect | 武器特效 |
| GunFov | 武器FOV |
| GunGunkick | 武器后坐力 |
| GunGunsway | 武器摇摆 |
| GunMovesway | 移动摇摆 |
| GunOffset | 武器偏移 |
| GunPerfor | 穿透数据 |
| GunRecoil | 后坐力 |
| GunSpread | 散布 |
| GunViewkick | 视角后坐 |
| GunViewsway | 视角摇摆 |

---

## 八、其他实体类型（世界数组遍历用）

| 实体类名 | 说明 |
|----------|------|
| `PlayerEntity` | 玩家 |
| `CarEntity` | 汽车 |
| `HorseEntity` | 马 |
| `BulletEntity` | 子弹 |
| `MissileEntity` | 导弹/火箭 |
| `NPCEntity` | NPC |
| `OreEntity` | 矿石 |
| `TreeEntity` | 树木 |
| `CollectableEntity` | 可拾取物 |
| `StorageDebrisEntity` | 储存碎片 |
| `TrapEntity` | 陷阱 |
| `ShopEntity` | 商店 |
| `MonumentEntity` | 纪念碑 |
| `SpecializedVehicleEntity` | 特殊载具 |
| `TrainBarricadeEntity` | 火车路障 |
| `EffectEntity` | 特效 |
| `WildEntity` | 野生动物 |
| `ObserverEntity` | 观察者 |
| `InteractionEntity` | 交互物 |
| `ElevatorEntity` | 电梯 |
| `CaveLiftEntity` | 洞穴电梯 |

---

## 九、绘制实战指南

### ESP绘制流程

```
1. 获取 libunity.so 基址
2. 读取世界数组指针链 → Entity列表
3. 读取矩阵指针链 → 16float (4x4矩阵)
4. 遍历Entity列表:
   a. 判断Entity类型 (PlayerEntity / CarEntity / ...)
   b. 读取坐标 (_PosX/Y/Z_Smooth: 0x5C/0x60/0x64)
   c. W2S转换 (用矩阵计算屏幕坐标)
   d. 读取血量 (DamageableComponent)
   e. 读取状态 (MoveState / PoseState / IsDead)
   f. 读取武器 (HeldItemCustom)
   g. 绘制到屏幕
```

### 关键判断条件

```c
// 判断是否为玩家实体
if (EntityHash == PlayerEntity::CLASS_HASH) { ... }

// 判断是否死亡
bool isDead = *(bool*)(entity + 0x10D);    // IsDead
byte poseState = *(byte*)(entity + PoseState_Offset); // Dying = 2

// 判断是否开火
bool isInFire = *(bool*)(entity + 0x179);  // IsInFire

// 判断是否换弹
bool isReloading = *(bool*)(heldItem + 0x70); // IsReloading

// 判断是否开镜
byte adsState = *(byte*)(entity + 0x268);  // AdsAnimState

// 判断是否在空中
bool isInAir = *(bool*)(entity + 0x350);   // IsInAir

// 判断是否敌对
bool isHostile = *(bool*)(entity + 0x330); // lastIsHostile

// 判断是否本地玩家
bool isLocal = *(bool*)(entity + 0x2C0);   // bLocalPlayer
```

### W2S 世界转屏幕

```c
// 读取 4x4 矩阵 (16个float)
float matrix[16];
ReadMatrix(base + 0xABB80 + 0x40 + 0x10 + 0x110, matrix, 64);

// W2S 转换
bool WorldToScreen(float* matrix, Vector3 pos, float screenW, float screenH, Vector2* out) {
    float clipX = pos.x * matrix[0] + pos.y * matrix[4] + pos.z * matrix[8]  + matrix[12];
    float clipY = pos.x * matrix[1] + pos.y * matrix[5] + pos.z * matrix[9]  + matrix[13];
    float clipW = pos.x * matrix[3] + pos.y * matrix[7] + pos.z * matrix[11] + matrix[15];

    if (clipW < 0.1f) return false; // 在相机后面

    float ndcX = clipX / clipW;
    float ndcY = clipY / clipW;

    out->x = (ndcX + 1.0f) * 0.5f * screenW;
    out->y = (1.0f - ndcY) * 0.5f * screenH;
    return true;
}
```

---

## 九-B、方框ESP绘制（核心！）

### 原理

用脚底坐标和头部坐标分别W2S，得到屏幕上的上下两个点，再根据距离缩放宽度画方框。

### 完整代码

```c
// ============ 1. 读取坐标 ============
float posX = *(float*)(playerEntity + 0x5C);  // _PosX_Smooth
float posY = *(float*)(playerEntity + 0x60);  // _PosY_Smooth
float posZ = *(float*)(playerEntity + 0x64);  // _PosZ_Smooth

// 脚底坐标（原始Y）
Vector3 footPos = { posX, posY, posZ };

// 头部坐标（Y + 人物身高）
// 失控进化角色身高约 1.8m (Unity单位)
Vector3 headPos = { posX, posY + 1.8f, posZ };

// ============ 2. W2S转换 ============
Vector2 screenFoot, screenHead;
if (!W2S(matrix, footPos, screenW, screenH, &screenFoot)) continue; // 在身后跳过
if (!W2S(matrix, headPos, screenW, screenH, &screenHead))  continue;

// ============ 3. 计算方框大小 ============
float boxHeight = screenFoot.y - screenHead.y;   // 屏幕上的像素高度
if (boxHeight < 5.0f) continue;                    // 太小跳过（太远了）

float boxWidth = boxHeight * 0.45f;               // 宽高比约0.45（正常人体比例）

// 中心点
float centerX = screenHead.x;
float centerY = screenHead.y + boxHeight / 2.0f;

// 方框四角
float left   = centerX - boxWidth / 2.0f;
float right  = centerX + boxWidth / 2.0f;
float top    = screenHead.y;
float bottom = screenFoot.y;

// ============ 4. 绘制方框 ============
DrawRect(left, top, right, bottom, color);           // 普通方框
// 或者画骨骼线（见下方）
```

### 蹲下/倒地时高度调整

```c
byte poseState = *(byte*)(entity + PoseState_Offset);

float heightOffset = 1.8f; // 默认站立高度
switch (poseState) {
    case 1: heightOffset = 1.2f; break; // Crouch 蹲下
    case 2: heightOffset = 0.5f; break; // Dying 倒地
    case 3: heightOffset = 0.8f; break; // Swim 游泳
    case 5: heightOffset = 1.0f; break; // Dive 潜水
    case 8: heightOffset = 1.2f; break; // Drive 驾驶
    default: heightOffset = 1.8f; break; // Stand/Mantle等
}
```

---

## 九-C、射线（视线方向）绘制

### 原理

用 `_RotateY_Smooth`（偏移0x94）获取玩家朝向Yaw角，从头部坐标向前延伸一条线。

### 完整代码

```c
// ============ 1. 读取朝向 ============
float yaw = *(float*)(playerEntity + 0x94);  // _RotateY_Smooth

// ============ 2. 计算前方坐标 ============
// Unity使用弧度制，Yaw顺时针
// 注意：Unity的Yaw方向可能需要取反，具体看游戏坐标系
float rad = yaw * DEG2RAD;  // 如果yaw是角度，转弧度

// 射线长度（米）
float rayLen = 3.0f;

Vector3 footPos = { posX, posY, posZ };
Vector3 headPos = { posX, posY + 1.6f, posZ }; // 眼睛高度约1.6m

// 计算射线终点（水平面上的前方）
Vector3 rayEnd = {
    headPos.x + sinf(rad) * rayLen,
    headPos.y,                     // 水平射线，Y不变
    headPos.z + cosf(rad) * rayLen
};

// ============ 3. W2S转换并绘制 ============
Vector2 screenHead, screenRayEnd;
if (W2S(matrix, headPos, screenW, screenH, &screenHead) &&
    W2S(matrix, rayEnd, screenW, screenH, &screenRayEnd)) {
    DrawLine(screenHead.x, screenHead.y,
             screenRayEnd.x, screenRayEnd.y,
             COLOR_YELLOW, 2);
}
```

### 开镜射线（更精确的开镜检测）

```c
// 读取开镜状态
byte adsState = *(byte*)(playerEntity + 0x268);  // AdsAnimState
bool isADS = (adsState == ADS_ON_STATE);          // 具体枚举值需实测

if (isADS) {
    // 开镜时画红色粗射线
    DrawLine(screenHead.x, screenHead.y,
             screenRayEnd.x, screenRayEnd.y,
             COLOR_RED, 3);
}
```

---

## 九-D、骨骼ESP绘制

### 原理

通过 ClientPlayerGo 的骨骼 Transform 指针读取骨骼世界坐标，W2S后连线。

### 代码

```c
// 需要先拿到 ClientPlayerGo 指针
// ClientPlayerGo 骨骼偏移:
//   HeadBone    = Go + 0xD8  (Transform指针)
//   BodyBone    = Go + 0xE0
//   SpineBone   = Go + 0xE8
//   LeftFoot    = Go + 0xF0
//   RightFoot   = Go + 0xF8
//   Bip01Neck   = Go + 0x100

// Unity Transform 读取世界坐标:
// Transform内部结构:
//   +0x38 (或0x90) = m_LocalPosition / WorldPosition
//   具体偏移需要看Unity版本，通常:
//   Transform + 0x90 = position (Vector3)  ← 2022版本
//   或者通过 Transform.get_position() 虚函数调用

// 简化方式：直接用PlayerEntity坐标推算骨骼
Vector3 footPos = { posX, posY, posZ };
Vector3 pelvisPos = { posX, posY + 0.9f, posZ };
Vector3 spinePos = { posX, posY + 1.2f, posZ };
Vector3 neckPos  = { posX, posY + 1.5f, posZ };
Vector3 headPos  = { posX, posY + 1.7f, posZ };

// 画骨骼连线
Vector2 sFoot, sPelvis, sSpine, sNeck, sHead;
W2S(matrix, footPos, ...);
W2S(matrix, pelvisPos, ...);
W2S(matrix, spinePos, ...);
W2S(matrix, neckPos, ...);
W2S(matrix, headPos, ...);

DrawLine(sFoot.x, sFoot.y, sPelvis.x, sPelvis.y, COLOR_WHITE);
DrawLine(sPelvis.x, sPelvis.y, sSpine.x, sSpine.y, COLOR_WHITE);
DrawLine(sSpine.x, sSpine.y, sNeck.x, sNeck.y, COLOR_WHITE);
DrawLine(sNeck.x, sNeck.y, sHead.x, sHead.y, COLOR_WHITE);
```

---

## 九-E、距离计算 + 血量条

```c
// ============ 距离 ============
// 本地玩家坐标
float myX = *(float*)(myPlayerEntity + 0x5C);
float myY = *(float*)(myPlayerEntity + 0x60);
float myZ = *(float*)(myPlayerEntity + 0x64);

float dx = posX - myX;
float dy = posY - myY;
float dz = posZ - myZ;
float distance = sqrtf(dx*dx + dy*dy + dz*dz);

// 显示距离文字
char distText[32];
sprintf(distText, "%.0fm", distance);
DrawText(screenFoot.x, screenFoot.y + 5, distText, COLOR_WHITE);

// ============ 血量条 ============
// 需要读取 DamageableComponent 的 Hp 和 MaxHp
float hp = ...;     // 当前血量
float maxHp = ...;  // 最大血量
float hpRatio = hp / maxHp;

// 血量条位置（方框上方）
float barWidth = boxWidth;
float barHeight = 3.0f;
float barX = left;
float barY = top - 6.0f;

// 底色（灰色）
DrawFilledRect(barX, barY, barX + barWidth, barY + barHeight, COLOR_GRAY);
// 血量（绿色→黄色→红色）
uint32_t hpColor = (hpRatio > 0.5f) ? COLOR_GREEN :
                   (hpRatio > 0.25f) ? COLOR_YELLOW : COLOR_RED;
DrawFilledRect(barX, barY, barX + barWidth * hpRatio, barY + barHeight, hpColor);
```

---

## 九-F、完整绘制循环伪代码

```c
void ESPLoop() {
    uintptr_t base = get_module_base("libunity.so");

    // 1. 读矩阵
    float matrix[16];
    uintptr_t matPtr = base + 0xABB80;
    matPtr = *(uintptr_t*)(matPtr + 0x40);
    matPtr = *(uintptr_t*)(matPtr + 0x10);
    matPtr = matPtr + 0x110;
    memcpy(matrix, (void*)matPtr, 64);

    // 2. 读本地玩家
    uintptr_t myPlayer = GetLocalPlayer(); // 通过人物数组或MyPlayerClient
    float myX = *(float*)(myPlayer + 0x5C);
    float myY = *(float*)(myPlayer + 0x60);
    float myZ = *(float*)(myPlayer + 0x64);

    // 3. 遍历人物数组
    uintptr_t playerArray = GetPlayerArray(); // 你提供的人物数组偏移链
    int count = GetArrayCount(playerArray);

    for (int i = 0; i < count; i++) {
        uintptr_t playerEntity = GetArrayElement(playerArray, i);
        if (!playerEntity) continue;

        // 跳过自己
        bool isLocal = *(bool*)(playerEntity + 0x2C0);
        if (isLocal) continue;

        // 跳过死亡
        bool isDead = *(bool*)(playerEntity + 0x10D);
        if (isDead) continue;

        // 读坐标
        float posX = *(float*)(playerEntity + 0x5C);
        float posY = *(float*)(playerEntity + 0x60);
        float posZ = *(float*)(playerEntity + 0x64);

        // W2S
        Vector3 foot = { posX, posY, posZ };
        Vector3 head  = { posX, posY + 1.8f, posZ };
        Vector2 sFoot, sHead;
        if (!W2S(matrix, foot, screenW, screenH, &sFoot)) continue;
        if (!W2S(matrix, head, screenW, screenH, &sHead))  continue;

        // 方框
        float h = sFoot.y - sHead.y;
        float w = h * 0.45f;
        DrawRect(sHead.x - w/2, sHead.y, sHead.x + w/2, sFoot.y, COLOR_GREEN);

        // 射线
        float yaw = *(float*)(playerEntity + 0x94);
        float rad = yaw * DEG2RAD;
        Vector3 rayStart = { posX, posY + 1.6f, posZ };
        Vector3 rayEnd   = { posX + sinf(rad)*3, posY + 1.6f, posZ + cosf(rad)*3 };
        Vector2 sRayS, sRayE;
        if (W2S(matrix, rayStart, screenW, screenH, &sRayS) &&
            W2S(matrix, rayEnd, screenW, screenH, &sRayE)) {
            DrawLine(sRayS.x, sRayS.y, sRayE.x, sRayE.y, COLOR_YELLOW);
        }

        // 距离
        float dist = sqrtf(powf(posX-myX,2) + powf(posY-myY,2) + powf(posZ-myZ,2));
        char text[64];
        sprintf(text, "%.0fm", dist);
        DrawText(sFoot.x, sFoot.y + 5, text, COLOR_WHITE);

        // 血量条
        DrawHealthBar(sHead.x - w/2, sHead.y - 6, w, hp, maxHp);

        // 状态
        bool isFiring = *(bool*)(playerEntity + 0x179);
        if (isFiring) DrawText(sHead.x, sHead.y - 15, "FIRING", COLOR_RED);

        byte pose = *(byte*)(playerEntity + PoseState_Offset);
        if (pose == 1) DrawText(sHead.x, sHead.y - 25, "CROUCH", COLOR_YELLOW);
        if (pose == 2) DrawText(sHead.x, sHead.y - 25, "DYING", COLOR_ORANGE);
    }
}
```

---

## 十、GG修改器速查表

### 速度修改

| 目标 | 搜索类型 | 搜索方式 | 说明 |
|------|----------|----------|------|
| 移速 | Float | 未知初始值→跑搜增大→停搜不变 | 反复筛选 |
| 跳跃 | Float | 跳前搜→跳后搜增大 | |
| 重力 | Float | 搜1.0附近 | 默认1.0 |

### 血量修改

| 目标 | 搜索类型 | 搜索方式 | 说明 |
|------|----------|----------|------|
| 当前血量 | Float | 被打搜减小→回血搜增大 | |
| 最大血量 | Float | 100.0附近 | 大多数是100 |

### 状态修改

| 目标 | 搜索类型 | 搜索方式 | 说明 |
|------|----------|----------|------|
| 死亡状态 | Byte | 死0活1 | |
| 开火状态 | Byte | 开火1停火0 | 偏移0x179 |
| 换弹状态 | Byte | 换弹1不换0 | |

---

## 十一、Hook注入参考

```c
// 获取基址
uintptr_t base = get_module_base("libunity.so");

// 读取世界数组
uintptr_t bss = base + 0x???; // bss段偏移
uintptr_t world = *(uintptr_t*)(bss + 0x79be8);
world = *(uintptr_t*)(world + 0x668);
world = *(uintptr_t*)(world + 0x4c8);
// ... 继续跟偏移链

// 读取矩阵
float viewMatrix[16];
uintptr_t matrixBase = *(uintptr_t*)(bss + 0xABB80);
matrixBase = *(uintptr_t*)(matrixBase + 0x40);
matrixBase = *(uintptr_t*)(matrixBase + 0x10);
matrixBase = matrixBase + 0x110;
memcpy(viewMatrix, (void*)matrixBase, 64);
```

---

> **免责声明**: 本文档仅供安全研究和学习目的，所有偏移数据来源于公开的反编译信息。请遵守相关法律法规，不要用于破坏游戏公平性的行为。
